using System;

namespace June82021
{
    class Program
    {
        static void Main(string[] args)
        {
            // int n = MyUtils.GetInteger("Enter n: ");
            //  int d = MyUtils.GetInteger("Enter d: ");


            // Fraction f = new Fraction(n,d);

            //  Console.WriteLine("f =" + f);

            //Fraction f = MyUtils.GetFraction("Enter fraction f: ");
            // Console.WriteLine("f: " + f);

            FractionProgram p = new FractionProgram();
            p.Run();

        }
    }
}
